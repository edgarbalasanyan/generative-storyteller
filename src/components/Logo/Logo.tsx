import Icon from "../Svgs/Icon";
import IconText from "../Svgs/IconText";
import "./Logo.scss"

const Logo = () => {
  return (
    <div className="logo">
      <Icon className="icon"/>
      <IconText className="iconText"/>
    </div>
  );
};

export default Logo;
